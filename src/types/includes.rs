#[derive(Debug, Clone)]
/// File inclusion definition. Used to track dependencies between files
pub struct IncludeDefinition {
    // pub path: String, --> Implement later if needed
    pub file: String
}
